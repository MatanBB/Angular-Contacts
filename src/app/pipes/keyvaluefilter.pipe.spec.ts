import { KeyvaluefilterPipe } from './keyvaluefilter.pipe';

describe('KeyvaluefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvaluefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
